import { PageShell } from "@/components/common/PageShell";

export default function ApiDocsPage() {
  return (
    <PageShell
      eyebrow="Модуль 9 · 30 функций"
      title="API & SDK"
      description="REST + GraphQL + WebSocket. Swagger/OpenAPI документация, OAuth2, webhook-подписки, HMAC signing, retry logic, SDK на JS и Python."
    />
  );
}
