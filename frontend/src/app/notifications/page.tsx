import { PageShell } from "@/components/common/PageShell";

export default function NotificationsPage() {
  return (
    <PageShell
      eyebrow="Модуль 10 · 30 функций"
      title="Уведомления"
      description="Push (Web Push API), email, Telegram, Discord, SMS. Центр уведомлений, тихий режим, расписание, Read receipts, typing indicators."
    />
  );
}
